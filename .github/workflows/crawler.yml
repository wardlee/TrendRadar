name: Get Hot News

on:
  schedule:
    - cron: "33 * * * *"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 1. 【焦土策略】删除 output 文件夹和数据库
      # 强迫脚本认为这是“第一次运行”，从而触发完整的初始化和生成逻辑
      - name: Clean Slate (Delete old data)
        run: |
          echo "Deleting output directory to force fresh generation..."
          rm -rf output/
          rm -f news.db

      - name: Verify config
        run: |
          if [ ! -f config/config.yaml ]; then
            echo "Error: Config missing"
            exit 1
          fi

      # 2. 运行爬虫
      - name: Run crawler
        env:
          STORAGE_BACKEND: local
          GITHUB_ACTIONS: true
        run: python -m trendradar

      # 3. 【全盘搜寻】寻找生成的 HTML 到底去哪了
      # 如果 index.html 没变，我们要看看是不是生成了别的名字的文件
      - name: 🔍 Find generated HTML (Debug)
        run: |
          echo "=== 正在寻找所有 .html 文件 ==="
          find . -name "*.html" -not -path "./.git/*" -print0 | xargs -0 ls -lh --time-style=long-iso
          
          echo "=== 检查根目录 index.html 内容摘要 ==="
          # 看看里面有没有今天的日期
          grep -o "2025-12-22" index.html || echo "❌ 根目录 index.html 里没找到今天的日期"

      # 4. 防止资源屏蔽 (必带)
      - name: Create .nojekyll
        run: touch .nojekyll

      # 5. 部署
      - name: Fix permissions
        run: |
          chmod -c -R +rX . | while read line; do echo "::warning title=Invalid Permissions Fixed::$line"; done

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
